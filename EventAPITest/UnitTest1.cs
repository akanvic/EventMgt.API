namespace EventAPITest
{
    public class UnitTest1
    {
        [Fact]
        public void Test1()
        {

        }
        [Fact]
        public async Task GetAllUsers_ReturnsAllUsers()
        {
            // Arrange
            //var options = new DbContextOptionsBuilder<MyDbContext>()
            //    .UseInMemoryDatabase(databaseName: "MyDb")
            //    .Options;
            //var dbContext = new MyDbContext(options);
            //dbContext.Users.Add(new User { Id = 1, Name = "Alice" });
            //dbContext.Users.Add(new User { Id = 2, Name = "Bob" });
            //dbContext.SaveChanges();
            //var controller = new UsersController(dbContext);

            //// Act
            //var result = await controller.GetAllUsers();

            //// Assert
            //Assert.IsInstanceOf<IEnumerable<User>>(result.Value);
            //Assert.AreEqual(2, result.Value.Count());
            //Assert.IsTrue(result.Value.Any(u => u.Id == 1));
            //Assert.IsTrue(result.Value.Any(u => u.Id == 2));
        }

    }
}